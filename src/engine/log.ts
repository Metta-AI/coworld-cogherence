// Placeholder — fleshed out in Task 12 (game loop + log). Defines the per-turn
// record stored in GameState.log. Keep this minimal until then.

/** One entry in the game log, recorded per turn. */
export interface TurnRecord {
  turn: number;
}
